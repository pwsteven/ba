{% extends 'base.html.twig' %}

{% block title %}{{ header_text }} - BA OnBoarding - Your Lawyers Ltd{% endblock %}

{% block body %}
    {{ include('blocks/title_breadcrumb.html.twig') }}
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-body">
                    {{ form_start(form, {'attr': {'class': 'forms-sample'}}) }}
                    <div class="form-group mb-30">
                        {{ form_label(form.monitorCredit) }}
                        {{ form_widget(form.monitorCredit) }}
                        <div class="error_message">{{ form_errors(form.monitorCredit) }}</div>
                    </div>
                    {# START CREDIT MONITOR BLOCK #}
                    <div id="credit_monitor_block"{% if form.vars.data.monitorCredit != 'YES' %} class="display_none"{% endif %}>
                        <div class="form-group mb-30">
                            {{ form_label(form.monitorCreditFile) }}
                            {{ form_widget(form.monitorCreditFile) }}
                            <div class="error_message">{{ form_errors(form.monitorCreditFile) }}</div>
                        </div>
                    </div>
                    {# END CREDIT MONITOR BLOCK #}
                    <div class="row">
                        <div class="col-sm-12 text-right">
                            <button type="submit" class="btn btn-primary mr-2 p5">Submit &amp; Continue <i class="ik ik-chevrons-right"></i></button>
                        </div>
                    </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>


{% endblock %}
